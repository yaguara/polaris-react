import React from 'react';
import PageActions from '../PageActions';

export default (
  <PageActions
    uxpId="pageactions-1"
    primaryAction={{content: 'Primary action'}}
    secondaryActions={[{content: 'Secondary action'}]}
  />
);
